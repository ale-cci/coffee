#!/usr/bin/env coffee
-- NOTE: working progress
import "./sys" as sys

alias PrintfArgs = struct {
    int arg0,
    str template,
}

void main() {
    PrintfArgs t
    t.template = "val: %d\0A"
    t.arg0 = 3

    sys.printf(t.template, t.arg0)
}
